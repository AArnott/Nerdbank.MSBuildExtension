version: '{build}'
skip_tags: true
image: Visual Studio 2017
configuration: Release
before_build:
- dotnet restore src
build_script:
- dotnet build src /nologo
- dotnet pack src --no-build /nologo
test_script:
- ps: src\test.ps1
artifacts:
- path: 'bin\**\*.nupkg'
  name: NuGet Package
